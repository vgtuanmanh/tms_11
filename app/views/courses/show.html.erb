<% provide(:title, 'Course Progress') %>
<h1><%= @user.name %> in Courses <%= @course.name %></h1>
<p>Name: <b><%= @user.name %></b></p>
<p>Email: <b><%= @user.email %></b></p>
<p>Course: <b><%= @course.name %></b></p>
<p>Description: <b><%= @course.description %></b></p>
<br>
<table class="table">
  <div class="course">
    <thead>
      <th>
        <b>Subject Name</b>
      </th>
      <th>
        <b>Subject Description</b>
      </th>
      <th>
        <b>Task</b>
      </th>
      <th>
        <b>Status</b>
      </th>
    </thead>
    <% a = @course.assignments.find_by user: @user %>
    <% byebug %>
    <% a.user_subjects.each do |us| %>
      <tr>
        <th style="color:blue">
          <%= subject.name %> &nbsp;
        </th>
        <th>
          <%= subject.description %> &nbsp;
        </th>
        <th><%= subject.tasks.count %> tasks</th>
        <th><%= subject.tasks.count %></th>
      </tr>
      <% subject.tasks.each do |task| %>
        <tr>
          <th></th>
          <th></th>
          <th>
            <%= link_to task.name, "#" %> &nbsp;
          </th>
          <th>
          </th>
        </tr>
      <% end %>
    <% end %>
  </div>
</table>

