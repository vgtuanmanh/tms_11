<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-8">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <h3>
          <%= @user.name %>
        </h3>

        <%= @user.email %>
      </h1>
    </section>
  </aside>
</div>

<%= render 'report' %>